//! Run the egui editor frame and apply inspector edits.

use elderforge_editor::Editor;
use elderforge_scene::Scene;

pub fn run(_editor: &mut Editor, _scene: &mut Scene) {
    // TODO: begin an egui frame (egui-winit), call editor.ui(ctx, scene),
    // then hand the output to the renderer's UI pass.
}
